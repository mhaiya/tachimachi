# Tachimachi ランディングページ 初期実装

## 概要

起業直後の企業向けサービス「Tachimachi」のランディングページを新規制作。
ホームページ・名刺・ロゴデザインをすぐに使える状態で提供するサービスの紹介ページ。

---

## 実装内容

### 1. ページ構成（HTML）

| セクション | 内容 |
|---|---|
| Header | ロゴ固定表示 |
| Hero | メインビジュアル + キャッチコピー |
| Problem | 課題提示（黒枠ボックス + 黒バー） |
| Solution | 解決策（３アイコン構成） |
| Features | 特徴・料金（女性画像 + 黄色ボックス） |
| CTA | 問い合わせセクション |
| Footer | ロゴ・著作権・ポリシーリンク |

---

### 2. デザイン・CSS

- **カラー変数**：`--color01`（イエロー）/ `--color02`（ブルー）/ `--black` / `--white` / `--dark` をCSS変数で管理
- **フォント**：LINE Seed JP（Google Fonts）を全体に適用
- **流体タイポグラフィ**：`clamp()` を全フォントサイズに導入（基準幅1280px）
- **Featuresセクション**：女性画像に重なる形で黄色ボックス・テキストを `position: absolute` で配置。背景は `linear-gradient` で青→グレーのグラデーション
- **CTAセクション**：`min-height: calc(100vh - 80px)` でフッター分を考慮した高さ設定
- **固定ヘッダー**：`position: fixed` でスクロールしても常に表示

---

### 3. アニメーション

- **フェードインアニメーション**：`IntersectionObserver` を使用し、スクロールで各要素が `translateY` + `opacity` でフェードイン
- **黒バーアニメーション**：`.problem_bar` が0.5秒遅れて左からスライドイン（`translateX`）
- **CTAボタンホバー**：背景ブラック→ホワイト、文字イエロー→ブラック、矢印アイコンも連動（transition統一）

---

### 4. レスポンシブ対応

#### タブレット（850px以下）
- `features_content` を `position: static` に切り替え、縦並びに変更

#### スマホ（480px以下）
- **固定CTAボタン**：画面下部に `position: fixed` で表示。フッター到達時に色変化（`IntersectionObserver` で制御）
- **Problemセクション**：すりガラス（`backdrop-filter: blur`）で視認性を確保
- **Featuresセクション**：女性画像を固定高さ280pxに切り取り、コンテンツを静的配置に変更
- **CTAセクション**：ボタン非表示・固定ボタンに一本化、フッターに余白追加（72px）

---

### 5. ファイル構成

```
webpage/
├── index.html
├── style.css
├── style_backup.css      # clamp導入前バックアップ
├── style_backup2.css     # features_inner max-width導入前バックアップ
└── img/
    ├── icon/             # SVGアイコン・ロゴ
    └── source/           # 写真素材
```

---

## レビュー観点

- [ ] 各ブレークポイントでのレイアウト崩れがないか
- [ ] アニメーションのタイミング・動作に違和感がないか
- [ ] スマホ固定ボタンの表示・色変化が正しく動作するか
- [ ] `clamp()` による文字サイズが各画面幅で適切か
- [ ] フッター・CTAの高さバランス

---

## 備考

- タブレット（870px）ブレークポイントは今後追加予定
- 大幅なデザイン変更・追加制作対応は別途
